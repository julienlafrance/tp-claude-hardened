% =============================================================================
% preamble.tex — charte graphique « Télécom Paris / IMT » (rapport académique sobre)
% Moteur : lualatex (fontspec). Inclus via pandoc --include-in-header.
% Style : noir, serif Latin Modern, une colonne, sections numérotées, en-tête/pied
% à filets, encadrés arrondis à bord fin, tables booktabs. Aucun aplat de couleur.
% =============================================================================

% ---- Couleurs (sobres : essentiellement du gris/noir) -----------------------
\usepackage{xcolor}
\definecolor{tpink}{RGB}{190,20,60}       % rouge Télécom (accents ultra-ponctuels)
\definecolor{boxframe}{RGB}{40,55,95}     % bord des encadrés (bleu nuit sobre)
\definecolor{rulecol}{RGB}{60,60,60}      % filets d'en-tête / pied
\definecolor{shadecolor}{RGB}{245,245,245}% fond des blocs de code (gris très clair)
\definecolor{codeframe}{RGB}{210,210,210}
\definecolor{linknavy}{RGB}{25,45,95}     % liens (bleu nuit discret)
\definecolor{g1}{RGB}{245,245,245}        % gris figures — fond neutre
\definecolor{g2}{RGB}{232,232,232}        % gris figures — accent léger
\definecolor{okgreen}{RGB}{20,110,60}     % vert sobre (durci/OK)
\definecolor{kored}{RGB}{150,35,35}       % rouge sobre (vulnérable/KO)

% ---- Polices (lualatex + fontspec) : défaut = Latin Modern (comme la réf.) ---
\usepackage{fontspec}
\setmonofont{DejaVu Sans Mono}[Scale=0.84]  % couvre → ─ │ ✓ ✗ des logs/annexes
\usepackage{microtype}

% ---- Symboles Unicode (flèches, glyphes de logs) hors verbatim --------------
\usepackage{pifont}
\usepackage{newunicodechar}
\newunicodechar{✅}{{\color{okgreen}\ding{51}}}
\newunicodechar{❌}{{\color{kored}\ding{55}}}
\newunicodechar{🛑}{{\color{kored}\ding{54}}}
\newunicodechar{✓}{{\color{okgreen}\ding{51}}}
\newunicodechar{✗}{{\color{kored}\ding{55}}}
\newunicodechar{→}{\ensuremath{\rightarrow}}
\newunicodechar{←}{\ensuremath{\leftarrow}}
\newunicodechar{↑}{\ensuremath{\uparrow}}
\newunicodechar{🎯}{{\ding{110}}}
\newunicodechar{⚠}{{\ding{224}}}
\newunicodechar{≈}{\ensuremath{\approx}}
\newunicodechar{≥}{\ensuremath{\geq}}
\newunicodechar{≤}{\ensuremath{\leq}}
\newunicodechar{×}{\ensuremath{\times}}
\newunicodechar{↔}{\ensuremath{\leftrightarrow}}
\newunicodechar{«}{\guillemotleft}
\newunicodechar{»}{\guillemotright}

% ---- Sections NUMÉROTÉES, noires (charte académique) -------------------------
\usepackage{titlesec}
\setcounter{secnumdepth}{2}   % numérote \section et \subsection ; pas au-delà
\setcounter{tocdepth}{2}      % TOC = sections + sous-sections
\titleformat{\section}{\normalfont\Large\bfseries}{\thesection}{0.7em}{}
\titleformat{\subsection}{\normalfont\large\bfseries}{\thesubsection}{0.6em}{}
% \subsubsection = mini-titre gras EN LIGNE, non numéroté (comme la réf.)
\titleformat{\subsubsection}[runin]{\normalfont\normalsize\bfseries}{}{0em}{}[\ :]
\titlespacing*{\section}{0pt}{16pt}{7pt}
\titlespacing*{\subsection}{0pt}{11pt}{4pt}
\titlespacing*{\subsubsection}{0pt}{7pt}{0.6em}

% ---- En-tête / pied à filets (sujet · Télécom Paris · page · programme) ------
\usepackage{fancyhdr}
\usepackage{lastpage}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\footnotesize\scshape DATA707 --- Cyber dans la Big Data + IA}
\fancyfoot[L]{\footnotesize Télécom Paris}
\fancyfoot[C]{\footnotesize\thepage}
\fancyfoot[R]{\footnotesize MS IA Expert Data \& MLOps}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0.4pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{rulecol}\leaders\hrule height \headrulewidth\hfill}}
\renewcommand{\footrule}{\hbox to\headwidth{\color{rulecol}\leaders\hrule height \footrulewidth\hfill}}

% ---- Encadrés arrondis à bord fin (mappe les blockquotes markdown) ----------
\usepackage[framemethod=default]{mdframed}
\newmdenv[
  backgroundcolor=white, linecolor=boxframe, linewidth=0.7pt,
  skipabove=8pt, skipbelow=8pt, leftmargin=0pt, rightmargin=0pt,
  innerleftmargin=10pt, innerrightmargin=10pt, innertopmargin=7pt,
  innerbottommargin=7pt, roundcorner=4pt
]{calloutbox}
\let\oldquote\quote \let\endoldquote\endquote
\renewenvironment{quote}{\begin{calloutbox}\small}{\end{calloutbox}}

% ---- Tables : booktabs, sobres ----------------------------------------------
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{etoolbox}
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.18}
\AtBeginEnvironment{longtable}{\footnotesize}
\AtBeginEnvironment{tabular}{\small}

% ---- Code : fond gris très clair + fin cadre, retour à la ligne -------------
\usepackage{fvextra}
\fvset{breaklines=true, breakanywhere=true, fontsize=\small, commandchars=\\\{\}}
% encadre les blocs de code (Shaded) d'un filet gris discret
\usepackage[framemethod=default]{mdframed}
\BeforeBeginEnvironment{Shaded}{\begin{mdframed}[backgroundcolor=shadecolor,linecolor=codeframe,%
  linewidth=0.5pt,innerleftmargin=7pt,innerrightmargin=7pt,innertopmargin=5pt,%
  innerbottommargin=5pt,skipabove=7pt,skipbelow=7pt,roundcorner=2pt]}
\AfterEndEnvironment{Shaded}{\end{mdframed}}

% ---- Liens discrets (bleu nuit) — appliqués via pandoc -V colorlinks --------
% (linkcolor/urlcolor/citecolor passés en ligne de commande = linknavy)

% ---- Paragraphes : indentation classique (comme la réf.) --------------------
%   -> on NE charge PAS parskip : alinéa en début de paragraphe.
\usepackage{enumitem}
\setlist{itemsep=1.5pt, topsep=3pt, parsep=0pt}
\usepackage{graphicx}
\usepackage{adjustbox}
\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta, fit, backgrounds, shapes.geometric, calc}

% ---- Styles TikZ (SOBRES : niveaux de gris + 1 accent vert/rouge) -----------
\tikzset{
  ebox/.style={draw=codeframe, fill=g1, rounded corners=2pt, align=center,
               font=\footnotesize, inner sep=5pt, line width=0.5pt},
  hbox/.style={draw=black!55, fill=g2, rounded corners=2pt, align=center,
               font=\footnotesize, inner sep=5pt, line width=0.8pt},
  dbox/.style={draw=kored, fill=kored!5, rounded corners=2pt, align=center,
               font=\footnotesize, inner sep=5pt, line width=0.6pt},
  gbox/.style={draw=okgreen, fill=okgreen!6, rounded corners=2pt, align=center,
               font=\footnotesize, inner sep=5pt, line width=0.6pt},
  grp/.style={draw=black!45, rounded corners=3pt, dashed, line width=0.6pt},
  grpr/.style={draw=kored!70, rounded corners=3pt, line width=0.7pt},
  grpg/.style={draw=okgreen!70, rounded corners=3pt, line width=0.7pt},
  fl/.style={-{Stealth[length=5pt]}, line width=0.6pt, font=\scriptsize},
  fld/.style={-{Stealth[length=5pt]}, line width=0.6pt, dashed, font=\scriptsize},
  lbl/.style={font=\scriptsize\bfseries},
}
